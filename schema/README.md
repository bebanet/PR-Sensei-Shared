# schema (C side)

This folder holds the existing compiled-into-firmware headers from PR-Sensei-Shared:
`include/schema.h` (DeviceState + enums) and `pr_espnow_wire.h` (ESP-NOW binary wire).
They are unchanged by this restructure. Keep them here; firmware consumes them via submodule.
